define("filter_githubcode/syntaxhighlighter",["jquery"],(function($){function lines(code){let lines=code.replace(/\n$/,"").split("\n"),newCode="";return lines.forEach((function(line){var raw;newCode+="<span class='line'>"+(raw=line,$("<div>").text(raw).html()+"</span>\n")})),newCode}return{init:function(){$("pre:has(code.githubcode)").each((function(){let extractedCode=function($block){let code=$block.find("code.githubcode").first();if(0===code.length)return null;let attrs={};return $.each(code.prop("attributes"),(function(){"class"!==this.name&&(attrs[this.name]=this.value)})),{code:code.text(),classes:code.attr("class")||"",attrs:attrs}}($(this));if(!extractedCode)return;window.console.log("Attributes:",extractedCode.attrs);let parsedCode=lines(extractedCode.code),newCode=(classes=extractedCode.classes,code=parsedCode,$("<code>").attr("class",classes).html(code));var classes,code;$(this).empty().append(newCode)}))}}}));

//# sourceMappingURL=syntaxhighlighter.min.js.map